# 2026-09-17 — Day 9

> 規則：每天至少 3 題全新題目；複習題不計入每日新題額度。出題時不先提供 Topic / Pattern / Hint，卡住後才逐層提示。

## 今日新題

| 題目 | 難度 | 結果 | 時間 | 評價 | 紀錄 |
|---|---|---|---:|---|---|
| 160. Intersection of Two Linked Lists | Easy | Accepted | 19:20 | B | 使用 Python。原本已想到先記錄 `headA` 經過的 node，再走 `headB` 檢查是否遇到同一個 node，但卡在 traversal 與完整收尾。拿 Hint 1 後補上 `a = a.next`、`b = b.next` 與 return 邏輯，成功 Accepted。也確認 Python `dict` / `set` 可以直接存 `ListNode` 物件本身，而不是只能存 `node.val`。 |
| 169. Majority Element | Easy | Accepted | 11:59 | A | 使用 Python。沒有拿 Hint，使用 dictionary 計數；某個值次數超過 `n/2` 時立即 return。O(n) time / O(n) extra space。 |
| 70. Climbing Stairs | Easy | Accepted | 9:39 | A | 使用 Python。沒有拿 Hint，自行找到 `s[i] = s[i-1] + s[i-2]` 的 recurrence，使用 DP array 完成。O(n) time / O(n) space。這是目前第一次碰到 recurrence / DP 類型仍能直接完成。 |

## 複習題（不計入每日 3 題）

| 題目 | 難度 | 結果 | 時間 | 評價 | 紀錄 |
|---|---|---|---:|---|---|
| 141. Linked List Cycle | Easy | Accepted | 7:56 | A（複習） | 使用 C++，沒有拿 Hint。這次直接寫出 Floyd slow/fast pointer：`slow` 每次走一步、`fast` 每次走兩步，O(n) time / O(1) space。相較 9/14 第一次做時需要 Hint 且先使用 node 上限 workaround，今天已能無提示完成標準解法。 |

## Day 9 觀察

Linked List 仍是需要持續練的區域，但進步很明顯：141 的 Floyd slow/fast 已能無提示重現；160 也已經自己想出「記錄 A 的 node，再檢查 B」的核心方向，只是在 traversal 基本動作上仍會漏掉 pointer 往後移。

Array / Hash Map 類型維持穩定，169 無提示完成。70 則首次接觸 recurrence / DP 形式就能獨立找到狀態轉移，之後可以再用不同題型驗證這是否能穩定遷移。
