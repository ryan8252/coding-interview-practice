# 2026-09-19 — Day 11

> 今天開始正式混入 Medium。規則仍是每天至少 3 題全新題目；複習題不計入新題額度；出題前不提供 Topic / Pattern / 解法方向。

## 今日新題

| 題目 | 難度 | 結果 | 時間 | 評價 | 紀錄 |
|---|---|---|---:|---|---|
| 3. Longest Substring Without Repeating Characters | Medium | Accepted | 50:30 | B | 第一題正式加入訓練的 Medium。前面約 30 分鐘嘗試只用 `max_l` / `l` 維護答案；47 分鐘左右拿 Hint 1。最後沒有直接採用標準 Hash Map / Set 解法，而是自行完成一個以目前合法 window 長度 `l` 為核心的解法：每個新字元掃描目前 window `range(i-l, i)` 找重複；若找到在 `j`，令 `l=i-j`，否則 `l+=1`。Accepted。屬於 sliding-window 思路，Time O(n²) / Space O(1)。後續要練習如何用額外資料結構把內層掃描消掉，做到 O(n)。 |
| 100. Same Tree | Easy | Accepted | 26:27 | B | 第一次正式碰 Binary Tree。查詢了「如何 traversal tree」與 Python stack；stack API 本身屬語法，但 tree traversal 已涉及核心基礎，因此記 B。之後自己完成兩棵樹同步 traversal / 比較並 Accepted。需要建立 `node.val`、`node.left`、`node.right`、DFS/stack、`None` 節點等基本模型。 |
| 69. Sqrt(x) | Easy | Accepted | 19:27 | C | 一開始沒有方向。Hint 1 提醒把問題視為找最大的整數 `a` 使 `a*a <= x`；Hint 2 明確指出使用 Binary Search。之後自行寫出主要 binary search，但 loop 結束後錯誤 `return mid`，在 `x=8` 回傳 3；經說明後理解此寫法結束時 `end` 才是最大的合法值，改成 `return end` 後 Accepted。代表 binary search 主體能寫，但 boundary / 最終回傳位置仍需補強。 |

## 複習題（不計入每日 3 題）

| 題目 | 難度 | 結果 | 時間 | 評價 | 紀錄 |
|---|---|---|---:|---|---|
| 125. Valid Palindrome | Easy | Accepted | 12:48 | A（複習） | 使用 C++，以左右 pointer 從兩端往中間比較。本輪未回報查題目解法或拿 Hint；能把先前 Python 的核心概念換語言重現。 |

## Day 11 觀察

今天難度明顯提高：第一題直接加入 Medium，另外首次正式接觸 Binary Tree，並用一題 binary-search-on-answer 類型測試 Binary Search 遷移。

- **Sliding Window**：3 題最後確實維護了合法 window，但目前是 O(n²) / O(1) 版本；已理解時間與空間 trade-off，下一步是用 `dict` / `set` 讓 window 移動時不用重新掃。
- **Binary Tree**：目前屬新領域，traversal 還需要查資料，不應急著要求完全無提示；先建立 traversal / DFS / stack / None handling。
- **Binary Search**：能在提示後寫出 `start/end/mid` 主體，但「找最大合法值」的 boundary 與 loop 結束後該回傳 `start` / `end` / `mid` 仍不穩。
- **Two Pointers**：125 換成 C++ 仍能穩定重現。

## 後續安排

- 每日新題可開始採 2 題 Easy + 1 題基礎 Medium 的節奏，但新領域初次接觸時不強求 Medium。
- 3. Longest Substring Without Repeating Characters 之後安排重做，目標是自行消掉內層掃描，做到 O(n)。
- Binary Tree 接下來安排短小 Easy 題，測試是否能不再查 traversal 基礎。
- Binary Search 需要專門複習 boundary：exact match、first/last valid、loop 結束時 `start/end` 的語意。
- 先前已記錄的 C++ pointer / object 初始化弱點仍保留，之後需安排短複習。